factorial(n)
{
    if (n == 0)       # Базовый случай - факториал 0 равен 1
    {
        return 1;
    }
    else
    {
        return n * factorial(n - 1);  # Рекурсивно вызываем factorial(n-1)
    }
}

# Вычисление факториала числа 5
result = factorial(5);
